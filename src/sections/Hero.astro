---
import ArrowIcon from "@/assets/arrow.svg";
import UserIcon from "@/assets/user.svg";
import { heroCards } from "@/consts/heroCards";
---

<section
  class="hero p-4 overflow-hidden min-h-screen flex items-center justify-center"
>
  <div class="w-full mx-auto max-w-6xl grid justify-center items-center gap-12">
    <article class="space-y-4 w-full flex items-center justify-center flex-col">
      <span
        class="rounded-full px-3 py-1 bg-gray-50/90 text-xs flex justify-center items-center gap-2 text-gray-600 font-semibold shadow-md text-center mb-8"
      >
        <span class="bg-green-400 size-2 rounded-full block animate-pulse"
        ></span>
        <span>Haz crecer tu dinero desde hoy</span>
      </span>
      <h1
        class="title-principal font-bold font-tenor-sans text-gray-900 text-balance lg:w-11/12 text-center"
      >
        Control de <span
          class="bg-gradient-to-br from-green-500 to-indigo-600 bg-clip-text text-transparent"
          >Finanzas</span
        >
      </h1>
      <p class="text-gray-600 text-balance p-hero text-center max-w-2xl">
        Organiza tus ingresos y gastos fácilmente. Haz seguimiento y alcanza tus
        metas.
      </p>
      <div class="btns flex justify-center items-center gap-4 pt-4">
        <a
          href="/login"
          class="px-4 py-2 rounded-full cursor-pointer font-semibold border-2 bg-gradient-to-b from-blue-700 to-blue-500 border-blue-500 text-white btn-shadow-primary flex items-center justify-center gap-2"
        >
          <span><UserIcon class="size-5 inline-block" /></span>
          <span>Registrarme</span>
        </a>
        <a
          href="/dashboard"
          class="p-2 px-3 inline-flex items-center text-gray-600 rounded-full overflow-hidden relative transition-all duration-300 hover:text-gray-800 hover:bg-white/50 before:content-[''] before:absolute before:w-0 before:h-[2px] before:bg-gray-600 before:bottom-0 before:left-0 before:rounded-full before:transition-all before:duration-300 hover:before:w-full hover:before:bg-gray-800 group"
        >
          <span>Ir a Dashboard</span>
          <span
            class="group-hover:-rotate-30 transition-transform duration-300"
          >
            <ArrowIcon class="size-5 ml-2 inline-block" />
          </span>
        </a>
      </div>
    </article>
    <article
      class="flex gap-4 items-center justify-center flex-wrap w-full max-w-2xl"
    >
      {
        heroCards.map((card) => (
          <div class="bg-white/60 shadow-md rounded-lg p-2 flex items-center gap-2 min-w-[265px] backdrop-blur-md">
            <figure
              class:list={[card.colors.bg, card.colors.text, "p-1 rounded-sm"]}
            >
              <card.icon class="size-8" />
            </figure>
            <div>
              <h3 class="font-semibold text-gray-800 text-sm font-tenor-sans">
                {card.title}
              </h3>
              <p class="text-gray-600 text-xs">{card.description}</p>
            </div>
          </div>
        ))
      }
    </article>
  </div>

  <style>
    h1 {
      font-size: clamp(2rem, 5vw, 3rem);
      line-height: 1.1;
    }
    .p-hero {
      font-size: clamp(1rem, 3vw, 1.25rem);
      line-height: normal;
    }
  </style>
</section>
